# Security

Report vulnerabilities via GitHub private vulnerability reporting on this
repository (Security → Report a vulnerability). Don't open public issues for
security problems.

The admin API is bearer-token gated (`ADMIN_TOKEN`) and intended for trusted
deployment behind TLS. The public API serves only published content.
